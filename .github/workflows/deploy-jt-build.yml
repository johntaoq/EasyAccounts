name: Deploy to jt-build

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: easyaccounts-prod-deploy
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate docker compose file
        run: docker compose -f docker-compose.yml config > /dev/null

      - name: Validate deploy secrets
        env:
          JT_BUILD_HOST: ${{ secrets.JT_BUILD_HOST }}
          JT_BUILD_USER: ${{ secrets.JT_BUILD_USER }}
          JT_BUILD_SSH_KEY: ${{ secrets.JT_BUILD_SSH_KEY }}
        run: |
          set -euo pipefail
          [ -n "${JT_BUILD_HOST}" ] || { echo "Missing secret: JT_BUILD_HOST"; exit 1; }
          [ -n "${JT_BUILD_USER}" ] || { echo "Missing secret: JT_BUILD_USER"; exit 1; }
          [ -n "${JT_BUILD_SSH_KEY}" ] || { echo "Missing secret: JT_BUILD_SSH_KEY"; exit 1; }

  deploy:
    needs: verify
    runs-on: ubuntu-latest
    steps:
      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.JT_BUILD_HOST }}
          username: ${{ secrets.JT_BUILD_USER }}
          key: ${{ secrets.JT_BUILD_SSH_KEY }}
          port: 22
          script_stop: true
          script: |
            set -euo pipefail

            APP_DIR="$HOME/services/easyaccounts/prod"
            PROJECT_NAME="easyaccounts-prod"
            REPO_URL="https://github.com/${{ github.repository }}.git"
            BRANCH="${{ github.ref_name }}"

            mkdir -p "$(dirname "$APP_DIR")"

            if [ ! -d "$APP_DIR/.git" ]; then
              git clone "$REPO_URL" "$APP_DIR"
            fi

            cd "$APP_DIR"
            git remote set-url origin "$REPO_URL"
            git fetch --prune origin
            git checkout -B "$BRANCH" "origin/$BRANCH"

            docker compose -p "$PROJECT_NAME" pull
            # Clean up a stale project name created by earlier workflow runs.
            docker compose -p prod down --remove-orphans || true
            docker compose -p "$PROJECT_NAME" up -d --remove-orphans
            docker compose -p "$PROJECT_NAME" ps

            docker image prune -f
